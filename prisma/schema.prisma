// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider      = "prisma-client-js"
  // Alpine Linux (musl) targets for multi-arch Docker builds.
  // Note: Prisma uses `linux-musl-...` (not `linux-musl-x86_64-...`) for amd64.
  binaryTargets = ["native", "linux-musl-openssl-3.0.x", "linux-musl-arm64-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Config {
  id        String   @id @default("default")
  sonarrUrl String?
  sonarrApiKey String?
  sonarrEnabled Boolean @default(false)
  radarrUrl String?
  radarrApiKey String?
  radarrEnabled Boolean @default(false)
  prowlarrUrl String?
  prowlarrApiKey String?
  prowlarrEnabled Boolean @default(false)
  tmdbApiKey String?
  updatedAt DateTime @updatedAt
  createdAt DateTime @default(now())
}

